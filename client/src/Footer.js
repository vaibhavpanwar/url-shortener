import React from "react";

const Footer = () => {
  return (
    <footer className="footer">
      <p>
        This is a url shortening web application built as a demo project using
        Reactjs for front end ,node js for backend, mongoDb for storing Urls.
      </p>
    </footer>
  );
};

export default Footer;
